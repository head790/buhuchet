Рассчитай налог для ИП.

Аргументы: $ARGUMENTS
Формат: период (опционально). Примеры: "Q1 2025", "2025", "Q3". Без аргументов — текущий квартал.

## Инструкции

1. Прочитай `data/profile.yaml` — определи налоговый режим
2. Прочитай `data/journal.csv` — просуммируй доходы и расходы за период
3. Прочитай `data/payments.yaml` — найди уплаченные авансы и взносы за период

## Определение периода

- Если указан квартал (Q1, Q2, Q3, Q4) — расчёт **нарастающим итогом** с начала года до конца указанного квартала
- Q1: январь-март, Q2: январь-июнь, Q3: январь-сентябрь, Q4: январь-декабрь
- Если указан год — годовой расчёт
- Без аргументов — текущий квартал нарастающим итогом

## Формулы расчёта

### УСН 6% (доходы)

```
налоговая_база = сумма_доходов_нарастающим_итогом
начисленный_налог = налоговая_база × 6%
вычет_взносов = min(уплаченные_взносы_в_периоде, начисленный_налог)
    # 100% вычет для ИП без сотрудников
налог_к_уплате = начисленный_налог − вычет_взносов − уплаченные_авансы
если налог_к_уплате < 0: переплата (зачтётся в следующем периоде)
```

### УСН 15% (доходы минус расходы)

```
налоговая_база = сумма_доходов − сумма_расходов (нарастающим итогом)
если налоговая_база < 0: налоговая_база = 0
начисленный_налог = налоговая_база × 15%
минимальный_налог = сумма_доходов × 1% (только за год, не за квартал)
итоговый_налог = max(начисленный_налог, минимальный_налог)  # только при годовом расчёте
налог_к_уплате = итоговый_налог − уплаченные_авансы
```

### Патент (ПСН)

```
стоимость_патента = из profile.yaml → патент.стоимость
вычет_взносов = min(уплаченные_взносы, стоимость_патента)  # 100% для ИП без сотрудников
к_уплате = стоимость_патента − вычет_взносов

Сроки оплаты:
- Патент до 6 месяцев: полная сумма до окончания срока патента
- Патент > 6 месяцев: 1/3 в течение 90 дней, 2/3 до окончания срока
```

### НПД (самозанятость)

```
# Определи тип клиента из clients.yaml (поле "тип": юрлицо, ип, физлицо)
# Доход без указания клиента → считай как доход от физлица
доход_от_физлиц × 4% + доход_от_юрлиц_и_ип × 6%

# Налоговый вычет (бонус): 10 000 руб. пожизненно
# Уменьшает ставку: 4% → 3% (от физлиц), 6% → 4% (от юрлиц)
# Пока бонус не исчерпан
```

## Формат вывода

```
РАСЧЁТ НАЛОГА: {режим}
Период: {период} (нарастающим итогом)

Доходы:          {сумма} руб.
Расходы:         {сумма} руб.        ← только для УСН 15%
Налоговая база:  {сумма} руб.

Начисленный налог ({ставка}%):  {сумма} руб.
Вычет страховых взносов:        −{сумма} руб.
Уплаченные авансы:              −{сумма} руб.

═══════════════════════════════════════
К УПЛАТЕ:  {сумма} руб.
═══════════════════════════════════════

Срок уплаты: {дата}
КБК (для справки): {кбк}
Оплата через ЕНП: КБК 18201061201010000510
```

## Справочные данные

КБК (для деклараций):
- УСН доходы: 18210501011011000110
- УСН доходы-расходы: 18210501021011000110
- Патент: 18210504010021000110
- Взносы: 18210202000011000160
- НПД: оплата через приложение «Мой налог»

Оплата всех налогов с 2023 — через ЕНП: КБК 18201061201010000510

## Предупреждение

В конце расчёта всегда добавляй:
"⚠️ Расчёт основан на данных из journal.csv и payments.yaml. Проверьте полноту данных. Это не является налоговой консультацией."
